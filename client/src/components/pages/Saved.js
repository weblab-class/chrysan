import React, { Component } from "react";

class Saved extends Component {
    constructor(props) {
      super(props);
      // Initialize Default State
      this.state = {};
    }
  
    componentDidMount() {
      // remember -- api calls go here!
    }
  
    render() {
      return (
          <div>
              this is the saved objects page kek!!
          </div>
      )
    }
}

export default Saved